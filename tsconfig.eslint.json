{
  // eslint complains that the example folders aren't in our tsconfig path,
  // so we give eslint it's own config file
  "extends": "./tsconfig.json",
  "include": ["packages/*/src/**/*", "examples/**/*"],
  "exclude": ["node_modules/**/*", "**/generated/*"]
}
